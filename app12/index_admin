import reflex as rx
from .backend.backend import States


class State(rx.State):
    """The app state."""

    ...


@rx.page(route="/index_admin", title="Administrador", on_load=States.check_auth)
def index() -> rx.Component:
    rx.link("applicable_fees_admin")


app = rx.App()
